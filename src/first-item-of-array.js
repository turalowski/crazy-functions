/**
 * Represents a function which returns first value of array.
 * @function
 * @param {Array} array - List of elements
 * @returns {any} - First item of received array
 */
export function firstItemOfArray(array) {
  return array[0];
}
